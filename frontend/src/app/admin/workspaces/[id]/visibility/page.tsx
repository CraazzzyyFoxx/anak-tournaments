"use client";

import { RouteWorkspaceSection } from "@/components/admin/workspace-settings/sectionMounts";

/** Visibility settings of the workspace named in the route. */
export default function VisibilitySettingsPage() {
  return <RouteWorkspaceSection section="visibility" />;
}
