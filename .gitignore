.idea/
.vscode/
.mypy_cache/
.pytest_cache/
__pycache__/
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.env
backend/env/*.env
!backend/env/*.env.example
backend/.cache/

.coverage
.coverage*
/app/.idea
/parser/.idea
logs/
# `logs/` above is for runtime logs. Any `logs` segment under the Next app tree
# is a route, not output — this used to name one exact hub path and silently
# dropped the file the moment that route moved.
!frontend/src/app/**/logs/
monitoring/secrets/*
!monitoring/secrets/.gitkeep
!monitoring/secrets/*.example
.kombai/
.ruff_cache/
.claude/
proxy/shadowsocks.json
!proxy/shadowsocks.json.example
proxy/xray.json
!proxy/xray.json.example

*.egg-info/

target/
debug/
.playwright-mcp/
frontend-redesign/

# Local agent scratch: worktrees, transcripts and one-off deploy helpers. Never
# belongs in the tree — a committed worktree pointer breaks every other checkout.
.aislop/