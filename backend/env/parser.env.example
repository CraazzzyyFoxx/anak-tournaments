PORT=8002
CHALLONGE_API_KEY=your_challonge_api_key
CHALLONGE_USERNAME=your_challonge_username
PROXY_IP=your_proxy_ip
PROXY_PORT=your_proxy_port
PROXY_USERNAME=your_proxy_username
PROXY_PASSWORD=your_proxy_password

# Subscription verification credentials for the scheduled collector and the admin
# re-check trigger. Same values as discord.env / auth.env. Unset means every live
# check resolves `unknown` (`bot_not_configured` / `twitch_client_not_configured`),
# which fails open — the sweep runs and proves nothing.
DISCORD_TOKEN=your_discord_bot_token
TWITCH_CLIENT_ID=your_twitch_client_id
