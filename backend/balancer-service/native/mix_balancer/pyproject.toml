[build-system]
requires = ["scikit-build-core", "pybind11", "cmake"]
build-backend = "scikit_build_core.build"

[project]
name = "mix-balancer"
version = "1.0.0"
description = "Fast C++ balance engine for game team distribution"
readme = "README.md"
requires-python = ">=3.13"
license = { text = "MIT" }

[tool.scikit-build]
cmake.version = ">=3.12"
cmake.build-type = "Release"
wheel.packages = ["mix_balancer"]
sdist.include = ["CMakeLists.txt", "pybind11_bindings.cpp", "mix_balancer.cpp", "mix_balancer.hpp"]

[tool.scikit-build.cmake.define]
PYTHON_VERSION_MIN_MINOR = "1"
